"""LLM book structure resolver v1 (plan §5.4): Pass A grammar, Pass B local
window decisions, Pass C rescue, bounded repair, and the orchestrator that
drives them against the chapter-side ``apply-decisions`` contract."""
